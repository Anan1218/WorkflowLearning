# Monorepo install: three editable packages; their third-party deps live in
# each pyproject.toml. pip's resolver satisfies the stello-idp-* inter-package
# dependencies from these local editables.
-e packages/core
-e packages/evals
-e apps/api

# Dev tooling
ruff>=0.6

# Phase 2 (optional, offline optimizer experiment) - install when you get there:
# dspy>=2.5
